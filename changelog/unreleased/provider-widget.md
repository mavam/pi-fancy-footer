---
title: Provider widget
type: feature
authors:
  - mavam
---

A new `provider` widget shows the provider id of the active model, so switching
between models from different providers is visible in the footer instead of
only changing the model name. The widget is hidden by default; enable it via
`/fancy-footer` or with `"provider": { "enabled": true }`.
